import { resend } from '@/lib/resend'; 

import verificationEmail from '../../emails/verficationEmail';

import { ApiResponse } from '@/types/apiResponse';

// Need to right the method to handle the sendVerification email 